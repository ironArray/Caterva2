{
  "name": "caterva2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "doc",
    "example": "examples"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run minify",
    "minify": "esbuild caterva2/services/static/build/*.js --minify --outdir=caterva2/services/static/build --allow-overwrite",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "AGPL",
  "devDependencies": {
    "sass": "1.77.6",
    "vite": "^7.1"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.8",
    "bootstrap": "^5.3"
  }
}
